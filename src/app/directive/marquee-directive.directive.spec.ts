import { MarqueeDirectiveDirective } from './marquee-directive.directive';

describe('MarqueeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MarqueeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
